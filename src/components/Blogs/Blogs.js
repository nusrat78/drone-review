import React from 'react';

const Blogs = () => {
    return (
        <div>
            <h1>1.What is contex API?</h1>
            <h1>Ans: The React Context API is a method for a React app to pass data through the component tree without having to pass props down manually at every level. It ables to exchange unique details in solving prop drilling from all levels of application. That means it is the another way to prop-drilling or moving props.</h1>
            <br />
            <h1>2.What is semantic tag?</h1>
            <h1>Semantic HTML tags is that allows to add meaning to markup so that search engines, screen readers, and web browsers can make sense of it. It supply data about the contents of those tags that goes abroad just how they look on a page.</h1>
            <br />
            <h1>3.difference between inline inline-block and block?</h1>
            <h1>Inline elements do not break the line but it takes space in entire line block elements break the line and takes the full width. Inline block is the middle of inline and block element. it doesn't start on a new line but it takes full width and able to set height and width</h1>

        </div>
    );
};

export default Blogs;